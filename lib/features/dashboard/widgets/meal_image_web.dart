/// Web stub — local file loading not supported on web.
library;

import 'package:flutter/material.dart';

Widget loadMealImage(String path, double size) {
  return const Icon(Icons.image_not_supported, color: Colors.grey);
}
